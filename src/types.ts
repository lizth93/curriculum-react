export interface Props {
  children?: React.ReactNode;
  className?: string;
}

export interface certifications {
  name: string;
  altText: string;
  logo: string;
  period: string;
  title: string;
  type: string;
  urlCertificate?: string;
}
